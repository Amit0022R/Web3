import { Counter } from './counter';

export const metadata = {
    title: 'Counter Page',  // Metadata for the Counter page        /
}

export default function CounterPage() {
        return (
            <Counter />
        )
}